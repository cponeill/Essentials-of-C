/* Adding the hash-include function */
#include <stdio.h>

void main (void) {

    int a;
    int b = 3;
    int c;

    a = 2;
    c = a + b;
    printf("The sum of adding %d and %d equals %d.\n", a, b, c);

}
